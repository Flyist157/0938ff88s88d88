[build-system]
requires = ["setuptools>=75", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mti"
version = "0.1.0"
description = "Media Trust Infrastructure: provenance-first media trust scoring and policy decisions."
readme = "README_MTI.md"
requires-python = ">=3.12"
license = { text = "Proprietary" }
authors = [{ name = "MTI" }]
dependencies = [
  "fastapi>=0.115.6",
  "uvicorn[standard]>=0.32.1",
  "pydantic>=2.10.3",
  "pydantic-settings>=2.7.0",
  "python-multipart>=0.0.20",
  "sqlalchemy>=2.0.36",
  "aiosqlite>=0.20.0",
  "cryptography>=44.0.0",
  "ulid-py>=1.1.0",
  "pillow>=11.0.0",
  "typer>=0.15.1",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.3.4",
  "httpx>=0.28.1",
  "ruff>=0.8.4",
]

[project.scripts]
mti-api = "mti.api.main:main"
mti-worker = "mti.worker:main"
mti-capture = "mti.capture.cli:app"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py312"
select = ["E", "F", "I", "UP", "B"]

